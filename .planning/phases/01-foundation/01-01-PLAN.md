---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup:
  - service: clerk
    why: "Authentication provider"
    env_vars:
      - name: NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
        source: "Clerk Dashboard -> API Keys"
      - name: CLERK_SECRET_KEY
        source: "Clerk Dashboard -> API Keys"
    dashboard_config:
      - task: "Create application"
        location: "Clerk Dashboard -> Applications"
      - task: "Configure OAuth (optional)"
        location: "Clerk Dashboard -> OAuth Providers"

must_haves:
  truths:
    - "Next.js 15 app runs on localhost:3000"
    - "TypeScript strict mode enabled"
    - "Tailwind CSS configured and working"
    - "Dev server starts without errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "next.config.ts"
      provides: "Next.js configuration"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "strict"
    - path: "tailwind.config.ts"
      provides: "Tailwind configuration"
    - path: "src/app/layout.tsx"
      provides: "Root layout"
    - path: "src/app/page.tsx"
      provides: "Home page"
---

<objective>
Scaffold the Next.js 15 project with TypeScript and Tailwind CSS, creating the foundational project structure.

Purpose: Establish the base application that all subsequent infrastructure (database, auth, UI) will be built upon.
Output: Working Next.js 15 application with TypeScript strict mode and Tailwind CSS
</objective>

<execution_context>
@C:/Users/ASUS/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ASUS/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project</name>
  <files>package.json, next.config.ts, tsconfig.json, tailwind.config.ts, postcss.config.mjs, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, src/lib/utils.ts</files>
  <action>
    Run the following to scaffold the Next.js 15 project:

    ```bash
    npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --yes
    ```

    This creates:
    - package.json with Next.js 15, TypeScript, Tailwind, ESLint
    - next.config.ts with TypeScript config
    - tsconfig.json with strict mode enabled
    - tailwind.config.ts with content paths configured
    - postcss.config.mjs for Tailwind processing
    - src/app/layout.tsx (root layout)
    - src/app/page.tsx (home page)
    - src/app/globals.css (global styles)
    - src/lib/utils.ts (utility functions)

    Per locked decisions: Feature-based organization, file colocation, module boundaries at top level.
  </action>
  <verify>
    Run `npm run dev` and verify:
    1. Dev server starts without errors on localhost:3000
    2. TypeScript compilation succeeds (no errors in terminal)
    3. Page loads in browser showing Next.js welcome content
  </verify>
  <done>
    Next.js 15 app scaffolded with:
    - TypeScript strict mode enabled
    - Tailwind CSS configured
    - ESLint configured
    - App Router structure in src/app/
    - Dev server runs successfully
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. `npx tsc --noEmit` passes with no TypeScript errors
3. `npm run build` succeeds (optional but good practice)
4. Visit localhost:3000 shows the Next.js page
</verification>

<success_criteria>
- [ ] Project scaffolded with `npx create-next-app@latest`
- [ ] TypeScript strict mode enabled in tsconfig.json
- [ ] Tailwind CSS configured in tailwind.config.ts
- [ ] App Router structure created in src/app/
- [ ] Dev server runs on localhost:3000 without errors
- [ ] Package.json contains all required dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
