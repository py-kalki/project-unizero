---
phase: 03-ai-discovery-core
plan: '03'
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/components/tools/tool-card.tsx
  - src/components/tools/tool-grid.tsx
  - src/components/tools/tool-search.tsx
  - src/components/tools/tool-filters.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - 'ToolCard displays tool name, description, category badge, pricing'
    - 'ToolGrid renders multiple ToolCards in responsive grid'
    - 'ToolSearch updates URL with debounced search query'
    - 'ToolFilters allows category and pricing selection via URL'
  artifacts:
    - path: 'src/components/tools/tool-card.tsx'
      provides: 'Individual tool display card'
      contains: 'Card, Badge, pricing display'
    - path: 'src/components/tools/tool-grid.tsx'
      provides: 'Grid layout of tools'
      contains: 'responsive grid, empty state'
    - path: 'src/components/tools/tool-search.tsx'
      provides: 'Search input with URL sync'
      contains: 'use-debounced-callback, useSearchParams'
    - path: 'src/components/tools/tool-filters.tsx'
      provides: 'Filter sidebar with category/pricing'
      contains: 'URL param updates, clear filters'
  key_links:
    - from: 'src/components/tools/tool-search.tsx'
      to: '/tools page'
      via: 'URL query params'
      pattern: 'replace.*searchParams'
    - from: 'src/components/tools/tool-filters.tsx'
      to: '/tools page'
      via: 'URL query params'
      pattern: 'replace.*category.*pricing'
---

<objective>
Create UI components for browsing, searching, and filtering the AI tool catalog. Components use URL-driven state for shareable, bookmarkable filter combinations.
</objective>

<execution_context>
@C:/Users/ASUS/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ASUS/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ai-discovery-core/03-RESEARCH.md
@src/lib/services/tool.service.ts
</context>

<tasks>

<task type="auto">
  <name>Create ToolCard component</name>
  <files>src/components/tools/tool-card.tsx</files>
  <action>
    Create src/components/tools/tool-card.tsx:
    1. Import Link from next/link, Card components from shadcn/ui, Badge
    2. Accept tool prop with type including name, slug, description, logoUrl, pricingType, monthlyPrice, category
    3. Create getPricingLabel helper function
    4. Render Card with:
       - Link to /tools/[slug]
       - Logo (if available) or placeholder
       - Tool name as CardTitle
       - Category Badge
       - Description (line-clamp-2)
       - Pricing label
    5. Add hover:shadow-md transition for interactivity
    
    Reference: 03-RESEARCH.md lines 437-496 for component pattern
  </action>
  <verify>Component renders without TypeScript errors</verify>
  <done>ToolCard component displays tool info with proper styling</done>
</task>

<task type="auto">
  <name>Create ToolGrid component</name>
  <files>src/components/tools/tool-grid.tsx</files>
  <action>
    Create src/components/tools/tool-grid.tsx:
    1. Import ToolCard component
    2. Accept tools prop (array)
    3. Render grid with responsive columns (grid-cols-1 md:grid-cols-2 lg:grid-cols-3)
    4. Map tools to ToolCard components
    5. Handle empty state - show "No tools found" message with clear filters action
    6. Include spacing with gap-6
    
    Reference: 03-RESEARCH.md lines 404-416 for empty state pattern
  </action>
  <verify>Component renders grid layout correctly</verify>
  <done>ToolGrid displays tools in responsive grid with empty state handling</done>
</task>

<task type="auto">
  <name>Create ToolSearch component with debounce</name>
  <files>src/components/tools/tool-search.tsx</files>
  <action>
    Create src/components/tools/tool-search.tsx:
    1. Use 'use client' directive
    2. Import useSearchParams, usePathname, useRouter from next/navigation
    3. Import useDebouncedCallback from use-debounced-callback (or implement simple debounce)
    4. Import Input from @/components/ui/input
    5. Accept initialValue prop for server-side initial state
    6. Create handleSearch function that:
       - Uses URLSearchParams
       - Sets 'q' param with search term
       - Resets page to 1 on new search
       - Uses replace() to update URL without reload
    7. Debounce search by 300ms
    8. Render Input with placeholder "Search AI tools..."
    
    Note: Install use-debounced-callback if needed: npm install use-debounced-callback
    
    Reference: 03-RESEARCH.md lines 150-184 for pattern
  </action>
  <verify>npm install use-debounced-callback succeeds, component has no TypeScript errors</verify>
  <done>ToolSearch updates URL with debounced search query</done>
</task>

<task type="auto">
  <name>Create ToolFilters component</name>
  <files>src/components/tools/tool-filters.tsx</files>
  <action>
    Create src/components/tools/tool-filters.tsx:
    1. Use 'use client' directive
    2. Import useSearchParams, usePathname, useRouter from next/navigation
    3. Import Button from @/components/ui/button, Badge for active filter count
    4. Define categories array (9 categories from research)
    5. Define pricingTypes array (FREE, FREEMIUM, SUBSCRIPTION, PER_TOKEN)
    6. Create updateFilter function:
       - Toggle filter on click
       - Update URL params (category, pricing)
       - Reset page to 1
    7. Create clearFilters function
    8. Show active filter state with highlighted buttons
    9. Render filter sections with headings
    
    Reference: 03-RESEARCH.md lines 501-602 for full implementation
  </action>
  <verify>Component renders with no TypeScript errors</verify>
  <done>ToolFilters allows category and pricing selection via URL</done>
</task>

</tasks>

<verification>
1. Verify all components compile without errors
2. Verify ToolSearch debounces input (300ms delay)
3. Verify ToolFilters updates URL without page reload
4. Verify empty state shows when no tools match
</verification>

<success_criteria>

- ToolCard displays name, description, category, pricing
- ToolGrid is responsive (1/2/3 columns)
- ToolSearch debounces and updates URL
- ToolFilters toggles category and pricing in URL
- Empty state handled gracefully
  </success_criteria>

<output>
After completion, create `.planning/phases/03-ai-discovery-core/{phase}-03-SUMMARY.md`
</output>
