---
phase: '02-authentication'
plan: '02'
type: 'execute'
wave: 2
depends_on:
  - '02-01'
files_modified: []
autonomous: false
user_setup:
  - service: clerk
    why: 'Enable OAuth providers (Google, GitHub) for social sign-in'
    dashboard_config:
      - task: 'Enable Google OAuth'
        location: 'Clerk Dashboard -> User & Authentication -> Social Connections -> Google'
        instructions: "Click Enable, use Clerk's shared credentials for dev or create custom credentials for prod"
      - task: 'Enable GitHub OAuth'
        location: 'Clerk Dashboard -> User & Authentication -> Social Connections -> GitHub'
        instructions: "Click Enable, use Clerk's shared credentials for dev or create custom credentials for prod"
      - task: 'Configure redirect URLs (production)'
        location: 'Clerk Dashboard -> User & Authentication -> OAuth'
        instructions: 'If using custom credentials, ensure callback URLs match your OAuth provider settings'
must_haves:
  truths:
    - 'Google OAuth appears as sign-in option on sign-in and sign-up pages'
    - 'GitHub OAuth appears as sign-in option on sign-in and sign-up pages'
  artifacts: []
  key_links: []
---

<objective>
Configure OAuth providers in Clerk Dashboard to enable Google and GitHub sign-in.

Purpose: Allow users to authenticate using their existing Google or GitHub accounts.
Output: OAuth buttons visible and functional on auth pages
</objective>

<execution_context>
@C:/Users/ASUS/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ASUS/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
# From 02-RESEARCH.md
- OAuth requires enabling in Clerk Dashboard (not code changes)
- Google and GitHub use Clerk's shared credentials in development
- Custom credentials needed for production
- OAuth buttons automatically appear in Clerk SignIn/SignUp components when enabled
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Enable OAuth providers in Clerk Dashboard</name>
  <files></files>
  <action>
    User must complete these steps in Clerk Dashboard:
    
    1. Go to https://dashboard.clerk.com
    2. Select your application
    3. Navigate to User & Authentication -> Social Connections
    4. Click Google -> Enable (use shared credentials for dev)
    5. Click GitHub -> Enable (use shared credentials for dev)
    6. Save changes
    
    For production, also:
    - Create OAuth credentials in Google Cloud Console and GitHub Developer Settings
    - Add callback URLs from Clerk Dashboard to your OAuth app settings
    - Enter credentials in Clerk Dashboard
  </action>
  <verify>Navigate to /sign-in page, Google and GitHub OAuth buttons should be visible</verify>
  <done>OAuth buttons appear on sign-in and sign-up pages, clicking initiates OAuth flow</done>
</task>

</tasks>

<verification>
1. Navigate to /sign-in - Google button should be visible
2. Navigate to /sign-in - GitHub button should be visible
3. Click Google - should redirect to Google OAuth
4. Click GitHub - should redirect to GitHub OAuth
</verification>

<success_criteria>

- AUTH-03: User can sign in with Google OAuth
- AUTH-04: User can sign in with GitHub OAuth
  </success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-02-SUMMARY.md`
</output>
